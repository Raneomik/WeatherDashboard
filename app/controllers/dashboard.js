'use strict';

/**
 * Dashboard module, for the dashboard controller got at '/dashboard' route
 */
angular.module('weatherApp.dashboard', [])
  .controller('dashboardCtrl', ['$scope',  function($scope) {

  }]);